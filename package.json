{
  "name": "yogue-aggregator-gateway",
  "version": "1.0.0",
  "description": "Stateless gateway in front of Yogue Pay's aggregator API. Forwards authenticated business-partner requests to the underlying provider — Yogue Pay today, others (e.g. Stripe) can be added behind the same client-facing routes later without partners changing their integration.",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "mongoose": "^8.5.1",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
